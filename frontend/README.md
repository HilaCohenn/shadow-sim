# Shadow Simulator Frontend

React + TypeScript client for the Shadow Simulator project.

Run it from this directory:

```bash
npm install
npm run dev
```

The full setup, usage notes, and API documentation are in the project README at the repository root.
